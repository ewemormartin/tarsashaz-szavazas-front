import { MeetingfilterPipe } from './meetingfilter-pipe';

describe('MeetingfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new MeetingfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
